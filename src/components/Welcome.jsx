// const Welcome = (props) => {
//   return <h1>Meet the <i>{props.name}</i></h1>
// }

// export default Welcome;

//OR with less code to write:

export default (props) => {
  return <h1>Meet the <i>{props.name}</i></h1>
}